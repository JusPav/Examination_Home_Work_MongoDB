table Students {
  ID integer [pk, not null, unique]
  NAME varchar(50) [not null]
  SURNAME varchar(50) [not null]
}
table Exam_By_Student {
  ID integer [pk, not null, unique]
  EXAM_NAME varchar(50) [not null]
  STUDENT_RATE integer
}
table Student_Answers {
  ID integer [pk, not null, unique]
  QUESTION_NUMBER integer
  ANSWER_BY_STUDENT varchar(2)
}


table Exams {
  ID integer [pk, not null, unique]
  NAME varchar(50) [not null]
  TYPE varchar(50) [not null]
}
table Exam_Answers {
  ID integer [pk, not null, unique]
  QUESTION_NUMBER integer
  CORRECT_ANSWER varchar(2)
}

Ref: "Exam_By_Student"."ID" < "Students"."ID"

Ref: "Student_Answers"."ID" < "Exam_By_Student"."ID"

Ref: "Exam_By_Student"."ID" < "Exams"."ID"

Ref: "Exam_Answers"."ID" < "Exams"."ID"